# Handling an urgent greet

When `greet` is called with `mood=urgent`, the standard response is to page whoever owns the
service and wait for acknowledgment before doing anything else. Do not escalate mood further —
`urgent` is the highest value this tool accepts.
